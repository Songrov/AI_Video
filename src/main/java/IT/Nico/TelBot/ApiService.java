package IT.Nico.TelBot;
/*
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class String {

    public void Test() {
        String url = "jdbc:mysql://localhost:3306/javabase";
        String username = "java";
        String password = "password";

        System.out.println("Connecting database ...");

        try (Connection connection = DriverManager.getConnection(url, username, password)) {
            System.out.println("Database connected!");
        } catch (SQLException e) {
            throw new IllegalStateException("Cannot connect the database!", e);
        }
    }

}

CREATE USER 'java'@'localhost' IDENTIFIED BY 'password';
GRANT ALL ON javabase.* TO 'java'@'localhost' IDENTIFIED BY 'password';


import org.springframework.stereotype.Service;
import org.springframework.web.client.RestTemplate;

@Service
public class ApiService {
    private final String API_URL = "https://api.example.com/data";
    private final String API_KEY = "luma-a25f4e10-8952-4f54-b75c-651fd6c2eb7b-c51e0790-9fd2-4e22-91d2-d32e9f283aaa";

    public String getData() {
        RestTemplate restTemplate = new RestTemplate();
        String url = API_URL + "?apikey=" + API_KEY;
        return restTemplate.getForObject(url, String.class);
    }
}

*/
